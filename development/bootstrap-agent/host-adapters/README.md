# Bootstrap Host Adapters

Host adapters describe how the stage-0 bootstrap runtime is discovered and
which capabilities the development host actually exposes. They do not change
project authority or product semantics.

The current reference adapter is [Codex](codex/README.md).
