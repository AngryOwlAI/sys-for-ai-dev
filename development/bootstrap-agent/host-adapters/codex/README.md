# Codex Bootstrap Adapter

Codex discovers generated bindings under `.agents/skills/` and
`.codex/skills/`. Each binding points to a canonical skill under
`development/bootstrap-agent/skills/`.

`reference-host-profile.toml` is development evidence for this host profile.
It is not shipped with the Sys4AI product and does not grant permissions.
