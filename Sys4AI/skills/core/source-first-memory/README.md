# Generic Source-First Memory Skill

This is a portable scaffold skill for target systems generated by Sys4AI.

It describes how a target system can use memory lookup as source navigation without turning memory into authority.

It is not the active `Sys4AI-dev` runtime skill. The active development-runtime skill lives at `.agents/skills/source-first-memory/`.
