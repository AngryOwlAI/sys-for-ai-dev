# Configuration Sources

This directory contains registered configuration examples and later project configuration sources for `Sys4AI`.

Phase 1 treats TOML as the preferred configuration-source format. Configuration files are controlled only when they have registry evidence in `registries/config_source_registry.csv` and pass the relevant validation contract.

No secret-bearing configuration belongs in Phase 1 examples.
