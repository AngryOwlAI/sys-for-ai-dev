# Domain packs

Domain packs are optional, versioned extensions conforming to
`contracts/schemas/domain-pack.schema.json` and
`contracts/policies/domain-packs.md`. No domain pack is bundled in the baseline
product, because domain claims require identified sources and accountable
review.
