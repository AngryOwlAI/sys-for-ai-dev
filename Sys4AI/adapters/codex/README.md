# Codex reference host

Codex is the first reference host. Its adapter declares observed capabilities;
project authority and transaction permissions still constrain every action.
The core contains no Codex-specific purpose or approval rule.
