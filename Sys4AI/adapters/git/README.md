# Git adapter

The baseline Git adapter exposes read-only status and diff observations.
Mutation requires a separately authorized tool adapter and is intentionally not
implicit in source-control access.
