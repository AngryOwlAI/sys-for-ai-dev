# Validation workflows

- `validate-development.yml` checks project control, trace, skills, and boundaries.
- `validate-product.yml` tests and builds the product across supported Python versions.
- `validate-integration.yml` exercises target generation and bounded execution.
