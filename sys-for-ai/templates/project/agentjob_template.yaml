agentjob_id: AJ-CHANGE-ME
objective: Replace this with one bounded unit of work.
role: replace_with_authorized_role
allowed_reads:
  - PRDs/**
allowed_writes:
  - sys-for-ai/**
forbidden_actions:
  - Replace source authority without a decision record.
expected_outputs:
  - Completion receipt.
validators:
  - cd sys-for-ai && make validate
stop_conditions:
  - Required source is missing.
  - Validation fails and cannot be repaired within the job boundary.
