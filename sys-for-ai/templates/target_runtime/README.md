# Target runtime templates

These files are placeholders for future target agentic systems generated by `sys-for-ai`.

They are not the Phase 1 development environment.

Use `.venv` for Phase 1 development. Expand these templates only when `sys-for-ai` begins producing deployable target systems that need containerized runtime scaffolding.
