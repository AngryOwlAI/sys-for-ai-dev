# Schemas

This directory contains Phase 1 schema-like specifications and executable validation contracts.

- `*.schema.yaml` files are retained scaffold specifications from the initial Phase 1 baseline.
- `contracts/*.schema.json` files are executable JSON Schema validation contracts.

JSON Schema validation proves structural admissibility. It does not prove domain correctness, semantic truth, or user acceptance.
